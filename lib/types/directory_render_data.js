/**
 * Render data for a directory.
 *
 * @typedef {Sermit~BaseRenderData} Sermit~DirectoryRenderData
 * @property {number} totalSize - sum of all `node` sizes in bytes.
 * @property {string} totalSizeBytes - `totalSize` formatted for human
 * @property {SermitNodeData[]} nodes - descriptors for all file & directory
 *   nodes in `path`.
 */
